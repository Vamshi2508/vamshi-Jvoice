node_modules/
# Service-account keys must never be committed. The seeding script prefers
# Application Default Credentials precisely so one is not needed.
*-service-account*.json
serviceAccountKey.json

# Generated by SeedExportTest in the Android project, and reproducible from it.
# 300 KB of content that already lives in the Kotlin sources it is exported from,
# so committing it would mean maintaining the same Telugu strings twice.
jvoice-seed.json

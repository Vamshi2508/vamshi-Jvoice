<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only used by Coil to load dummy demo image URLs. No backend / API calls. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Asked for once, on first launch, alongside the language choice. Nothing is
      actually pushed in the prototype - the grant is what the first-run dialogue
      needs so breaking-news and exam alerts can be wired up later.
      Runtime request only applies from API 33; below that the grant is implicit.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.JVoice">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.JVoice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
